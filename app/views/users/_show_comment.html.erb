<div class='well'>
  "<%= comment.body %>" on <%= link_to("#{comment.post.title}", post_path(comment.post)) %><br />
  posted by <%= link_to("#{comment.user.username}", user_path(@user)) %> at <%= comment.created_at %> 
</div>
